import SwiftUI

@main
struct DayPilotApp: App {
    var body: some Scene {
        MenuBarExtra("DayPilot", systemImage: "checklist.checked") {
            Text("DayPilot loading...")
                .padding()
        }
    }
}
